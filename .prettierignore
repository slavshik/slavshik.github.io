dist
node_modules
test/shots
test-results
playwright-report
test/e2e/__screenshots__
package-lock.json
.claude

# Разметка написана руками и выровнена руками: комментарии стоят напротив
# того, что объясняют, а атрибуты разложены так, чтобы голову было видно
# целиком. Prettier переклеивает и то, и другое, а взамен не даёт ничего —
# HTML тут не растёт и не генерируется.
*.html
