/*
 * Конфиги сборки и тесты: тут Node есть, и DOM тоже — page.evaluate()
 * исполняется в браузере, а пишется здесь.
 */
{
	"extends": "./tsconfig.json",
	"compilerOptions": {
		"types": ["node"]
	},
	"include": ["test", "scripts", "vite.config.ts", "playwright.config.ts", "eslint.config.js"]
}
